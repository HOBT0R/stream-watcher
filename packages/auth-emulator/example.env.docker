# Environment variables for the Firebase Auth Emulator service

# This value should match the VITE_FIREBASE_PROJECT_ID in the root .env file
# It tells the emulator which project to simulate.
FIREBASE_PROJECT_ID=stream-watcher-dev 