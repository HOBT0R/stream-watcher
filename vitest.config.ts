import { defineConfig } from 'vitest/config';

export default defineConfig({
    // This file is intentionally left minimal.
    // Each package in the workspace will have its own vitest.config.ts
}); 